# ===========================================
# RICE Tool Configuration
# ===========================================
# Copy this file to .env and configure for your deployment
# cp .env.example .env

# ===========================================
# DEPLOYMENT CONFIGURATION
# ===========================================
# Set deployment mode: 'local' for development, 'production' for live deployment
MODE=local

# ===========================================
# DOMAIN CONFIGURATION
# ===========================================
# For local development: localhost
# For production: your domain (e.g., mysite.com)
DOMAIN=localhost

# Admin email for SSL certificate notifications (production mode only)
ADMIN_EMAIL=admin@localhost

# ===========================================
# NOTES
# ===========================================
# Passwords, ports, and secrets are automatically generated during installation
# No need to configure them manually